#include "json_undefined.h"
#include "json_json.h"

/* Value &
Undefined::_assign (Undefined &nv)
{
  if (_parent)
    {
      _parent->assign (this, &nv);
      return *_parent;
    }

  return *this;
} */
